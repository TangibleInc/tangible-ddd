# tangible-ddd Integration Test Harness — Report

**Status: DONE**
**Date: 2026-06-22**

## What Was Built

### Harness files
- `tests/Integration/bootstrap.php` — boots WP via `wp-load.php` against `db_test`, then initialises the datastream DI container from `.reference/tangible-datastream/`
- `tests/Integration/IntegrationTestCase.php` — per-test transaction rollback isolation (`SET autocommit=0` + `START TRANSACTION` / `ROLLBACK`)
- `tests/Integration/CommandIntegrationTestCase.php` — command-bus base (no outer transaction; `cleanup_rows()` DELETE pattern; resets `EventsUnitOfWork` + `CorrelationContext` singletons)
- `phpunit.integration.xml` — PHPUnit 11 config pointing at `tests/Integration`

### E2E test
- `tests/Integration/Framework/CommandBusE2ETest.php` — drives the full pipeline:
  `RecordCapturedEventCommand` → `TransactionMiddleware` (START TX / COMMIT) → `DomainEventsPublishMiddleware` → `RecordCapturedEventHandler` → `CapturedEventRepository` (INSERT)

  **Assertions (15 total):**
  1. `captured_events` row persisted with correct `source`, `event`, `actor_id`
  2. `fields` JSON round-trips correctly
  3. `EventCaptured` WP action fired with correct payload
  4. `ICapturedEventRepository::get_by_id()` reconstitutes the aggregate

### Infrastructure notes
- `symfony/yaml` added as dev dependency (required by Symfony DI YAML loader)
- `.reference/` already gitignored; `.reference/tangible-datastream` created via `cp` inside ddev (no SSH to GitHub from ddev)
- Worktree symlinks `vendor` and `.reference` from the parent plugin dir

## Test Results

```
Integration: Tests: 1, Assertions: 15 — PASS
  Warning: WP core plugin.php:517 "Undefined array key 0" from add_action
  with named-param closure — harmless WP ≤6.x bug, all assertions green.

Unit: Tests: 215, Assertions: 494 — PASS (no regressions)
```

## Blockers
None. One warning (WP core, not our code).

## Deferred
- Task #14: Playwright E2E from ddev against a real site (separate concern)
- Additional integration tests for Destination CRUD, outbox delivery, subscription reconstitution

---

## 2026-06-22 — Outbox + WorkflowHandler Fork E2E tests

**Status: DONE**
**Commit: 250a5af** — "add E2E integration tests for outbox primitives and WorkflowHandler fork"

### Tests added (both pass)
- `tests/Integration/Framework/OutboxPrimitiveTest.php` — 5 tests, 20 assertions: write→pending, mark_completed, mark_failed (attempts++), move_to_dlq, OutboxProcessor success/DLQ paths
- `tests/Integration/Framework/BehaviourWorkflowForkTest.php` — 2 tests, 14 assertions: non-contiguous partial failures fork to child workflow; all-success completes without fork

### Results
Integration: Tests: 9, Assertions: 53 — PASS (1 unrelated surge-plugin warning)
Unit: Tests: 215, Assertions: 494 — PASS (no regressions)

### Blockers / notes
None. Two fixes during development: `$wpdb` visibility (inherited protected), and IIntegrationEvent requires `action()`+`payload()` from IDomainEvent. DLQ test primes `attempts=4` to hit `max_attempts=5` threshold.
